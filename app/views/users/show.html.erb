
<section class="hero is-success">
  <div class="hero-body">
    <div class="container">
      <h1 class="title">
        マイページ
      </h1>
    </div>
  </div>
</section>

<section class="section">
  <div class="container">
    <div class="columns is-centered">
      <div class="column is-8">
        <div class="columns is-centered">
          <div class="column is-4">
            <figure class="image is-128x128"  style="margin-left: auto; margin-right: auto;">
            <% if @user.image.attached?%>
              <%= image_tag @user.image, class: "profile_image is-rounded" %>
            <% end %>
            </figure>
          </div>
          <div class="column is-8">
            <table class="table is-fullwidth">
              <tr>
                <td class="is-size-4">
                  <strong><%= @user.nickname %></strong>
                </td>
                <% if @user.id == current_user.id %>
                <td class="is-size-4">
                  <%= @user.email %>
                </td>
                <td>
                  <%=link_to "編集", edit_user_path(@user), class: "button is-primary"%>
                </td>
                <% end %>
              </tr>
            </table>
            <table class="table is-fullwidth">
              <tr>
                <td>
                  <%= @user.profile %>
                </td>
              </tr>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<p>お気に入りの実装</p>
<p>お気にいりの公演情報とリンク</p>

<%#<section class="section">
  <div class="container">
    <div class="columns is-multiline">
      <% @user.performances.each do |perform| %>
        <%#<div class="column is-4">
          <div class="card">
            <div class="card-image">
              <figure class="image is-4by3">
                <%= link_to performance_path(perform) do %>
                <%#  <%= attachment_image_tag perform, :image, fallback: "no-image.png" %>
                <%#<% end %>
              <%#</figure>
            </div>
            <div class="card-content">
              <div class="media">
                <div class="media-content">
                  <%= link_to performance_path(perform), class: "panel-block" do %>
                    <%#<span class="panel-icon">
                      <i class="fas fa-book" aria-hidden="true"></i>
                    </span>
                    この公演情報を見る
                  <% end %>
                  <%#<% if @user.id == current_user.id %>
                    <%# <%= link_to edit_performance_path(perform), class: "panel-block" do %>
                    <%# <span class="panel-icon">
                        <i class="fas fa-edit"></i>
                      </span>
                      この公演情報を編集する
                    <% end %>
                    <%# <%= link_to performance_path(perform), method: :delete, data: {confirm: "削除しますか？"}, class: "panel-block" do %>
                      <%#<span class="panel-icon">
                        <i class="fas fa-trash"></i>
                      </span>
                      この公演情報を削除する
                    <% end %>
                  <%#<% end %>
                <%#</div>
              </div>
              <div class="content">
                <time><%= perform.updated_at.strftime("%Y-%m-%d %H:%M") %><%#</time>更新
              <%#</div>
            </div>
          </div>
        </div>
      <% end %>
    <%#</div>
  </div>
</section>
